import { Alert, Button, Chip, Paper, Snackbar, Table, TableBody, TableCell, TableContainer, TableHead, TableRow } from "@mui/material";
import { useState } from "react";
import AdminPageShell from "../components/admin/AdminPageShell";
import SuspensionReasonDialog from "../components/superAdmin/SuspensionReasonDialog";
const SuperAdminRAManagement = () => { const [dialog, setDialog] = useState<null | "suspend" | "delete">(null); const [notice, setNotice] = useState(""); return <AdminPageShell title="RA Governance" subtitle="Platform-level Research Analyst governance workflows."><Alert severity="warning" sx={{ mb: 2 }}>Actions below are confirmation-flow previews only and do not call a backend endpoint.</Alert><TableContainer component={Paper} variant="outlined"><Table size="small"><TableHead><TableRow><TableCell>Research Analyst</TableCell><TableCell>Registration</TableCell><TableCell>Status</TableCell><TableCell align="right">Governance preview</TableCell></TableRow></TableHead><TableBody><TableRow><TableCell>Aarav Mehta Research</TableCell><TableCell>INH000012345</TableCell><TableCell><Chip label="ACTIVE" size="small" color="success" /></TableCell><TableCell align="right"><Button color="warning" onClick={() => setDialog("suspend")}>Suspend</Button><Button color="error" onClick={() => setDialog("delete")}>Delete</Button></TableCell></TableRow></TableBody></Table></TableContainer><SuspensionReasonDialog open={dialog !== null} subject="Research Analyst" action={dialog ?? "suspend"} onClose={() => setDialog(null)} onConfirm={(reason) => setNotice(`Mock ${dialog} recorded with reason: ${reason}`)} /><Snackbar open={Boolean(notice)} autoHideDuration={5000} message={notice} onClose={() => setNotice("")} /></AdminPageShell>; };
export default SuperAdminRAManagement;
