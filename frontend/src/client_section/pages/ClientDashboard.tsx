import { Box, Typography } from "@mui/material";
import ClientRecommendation from "./ClientRecommendations";

const ClientDashboard = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">
        Client Dashboard
      </Typography>
    </Box>
  );
};

export default ClientDashboard;